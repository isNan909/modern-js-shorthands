// We can use  is with the backtick, and ${} to enclose your variables.

// Longhand syntax

const user = 'You have logged in as ' + firstName + ' ' + lastName + '.';

//Shorthand syntax

const user = `You have logged in as ${firstName} ${lastName};`;
